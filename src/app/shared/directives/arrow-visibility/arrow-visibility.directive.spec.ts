import { ArrowVisibilityDirective } from './arrow-visibility.directive';

describe('ArrowVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrowVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
