import { GoToNewsDirective } from './go-to-news.directive';

describe('GoToNewsDirective', () => {
  it('should create an instance', () => {
    const directive = new GoToNewsDirective();
    expect(directive).toBeTruthy();
  });
});
