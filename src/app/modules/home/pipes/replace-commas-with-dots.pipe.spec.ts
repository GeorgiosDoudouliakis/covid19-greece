import { ReplaceCommasWithDotsPipe } from './replace-commas-with-dots.pipe';

describe('ReplaceCommasWithDotsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceCommasWithDotsPipe();
    expect(pipe).toBeTruthy();
  });
});
