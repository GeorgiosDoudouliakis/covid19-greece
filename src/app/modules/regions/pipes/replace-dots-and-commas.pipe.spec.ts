import { ReplaceDotsAndCommasPipe } from './replace-dots-and-commas.pipe';

describe('ReplaceDotsAndCommasPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceDotsAndCommasPipe();
    expect(pipe).toBeTruthy();
  });
});
